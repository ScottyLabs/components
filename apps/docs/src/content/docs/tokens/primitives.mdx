---
title: Primitives
description: Raw palette colors. The semantic layer aliases these.
---

import PrimitivesGallery from "../../../components/PrimitivesGallery.astro";

The raw palette: eight color ramps with hex values. Consumers should usually reach for the semantic layer instead, which aliases these primitives by intent rather than by hue.

<PrimitivesGallery />
