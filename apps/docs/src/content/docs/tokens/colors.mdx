---
title: Semantic colors
description: Mode-aware aliases over the raw palette.
---

import SemanticColorsGallery from "../../../components/SemanticColorsGallery.astro";

Semantic tokens alias primitives by intent (`brand`, `neutral`, `brandNeutral`) and role (`background`, `foreground`, `stroke`, `status`). Each has a Light and Dark mode value; the Dark mode swatch is rendered alongside the Light.

<SemanticColorsGallery />
